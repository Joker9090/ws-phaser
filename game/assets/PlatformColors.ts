const colors = {
    rotate: Phaser.Display.Color.GetColor(255, 101, 255),
    gravity: Phaser.Display.Color.GetColor(255, 101, 101),
    // falling:Phaser.Display.Color.GetColor(0, 255, 0),
    falling: 0xffffff, // white, so tinting has no visible effect
}

export default colors
